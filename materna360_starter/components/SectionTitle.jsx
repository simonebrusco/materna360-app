export default function SectionTitle({ children }) {
  return (
    <h2 className="text-[15px] sm:text-[16px] font-semibold text-brand-ink/80 tracking-tight mb-3 sm:mb-4">
      {children}
    </h2>
  );
}
